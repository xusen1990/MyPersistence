<mapper namespace="product">

    <select id="selectList">
        select * from product
    </select>

    <select id="selectOne">
        select * from product where id = #{id} and username = #{username}
    </select>

</mapper>